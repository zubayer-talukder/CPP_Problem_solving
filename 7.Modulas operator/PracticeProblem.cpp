#include<iostream>
using namespace std;
int main(){
    float a = 5 , b = 2;
    int c;
    c = a % b;
    cout << c; 
    // remember the data type must be integer in value, for this reason program can't run anymore. 
}