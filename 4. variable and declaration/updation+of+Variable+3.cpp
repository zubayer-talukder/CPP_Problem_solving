#include<iostream>
using namespace std;
int main()
{
    int x =5;
   
    x = x + 30;
    /* x = x + 30 is not a mathemetical Equation,
    it means that in x variable
    we should add 30 in previous value of x*/
    cout << x << endl;

    x = x - 2;
    
    cout << x << endl;
// it means that in the previous result -2 will be added
    return 0;
}
